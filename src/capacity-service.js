export class CapacityService { constructor(store){this.store=store;} available(){const held=[...this.store.reservations.values()].filter(x=>x.state==='held').reduce((n,x)=>n+x.expectedKg,0);return this.store.zone.limitKg-this.store.zone.stockKg-held;} async reserve(input){if(this.available()<input.expectedKg)return {status:'rejected',reason:'capacity'};await Promise.resolve();this.store.reservations.set(input.id,{...input,state:'held'});return {status:'held'};} arrive(id,actualKg){const item=this.store.reservations.get(id);item.state='arrived';this.store.zone.stockKg+=actualKg;return {status:'arrived'};} cancel(id){const item=this.store.reservations.get(id);item.state='cancelled';return {status:'cancelled'};} }
